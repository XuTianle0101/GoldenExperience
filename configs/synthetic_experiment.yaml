# Legacy synthetic cache-store config retained for unit/prototype experiments.
# The active project direction is configs/cross_model_reuse.yaml.
experiment:
  name: goldenexperience_legacy_synthetic_store
  status: legacy
  note: "Use only for in-repo cache-core and tiered-store prototype tests."
