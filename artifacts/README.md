# Artifacts

This directory is reserved for generated benchmark assets.

- `cache/`: local NVMe-tier cache files from synthetic and model-backed runs.
- `results/`: JSON or CSV benchmark outputs.
- `figures/`: generated paper figures.

Large generated files are ignored by default. Keep only small metadata, manifests, or
curated result summaries in version control.

